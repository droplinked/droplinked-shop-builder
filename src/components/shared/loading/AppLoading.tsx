import { CircularProgress } from '@chakra-ui/react'
import React from 'react'

function AppLoading() {
    return (
        <CircularProgress isIndeterminate color='#2EC99E' thickness={8} />
    )
}

export default AppLoading