import { chakra } from "@chakra-ui/react";

export const CardTitle = chakra("p", {
    baseStyle: {
        fontSize: "20px",
        fontWeight: "bold",
        fontFamily:"aven",
        color: "#fff",
    },
});