import { chakra } from "@chakra-ui/react";

 const MainThemeImage = chakra("img", {
  baseStyle: {
    w:'100%' ,
    h:'auto',
    borderRadius:'8px',
    overflow:'hidden',
  },
});

export const DesignPageStyles = { MainThemeImage }
