import { chakra } from "@chakra-ui/react";

export const MerchpageContainer = chakra("div", {
  baseStyle: {
    width: " 100%",
    display: "flex",
    flexDirection: "column",
    height: "auto",
  },
});
