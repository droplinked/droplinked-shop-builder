import React from 'react'

interface Props {
    handleChange: (key: string, value: string) => void;
}

export default function ResultTabContent({ handleChange }: Props) {
    return (
        <div>ResultTabContent</div>
    )
}
